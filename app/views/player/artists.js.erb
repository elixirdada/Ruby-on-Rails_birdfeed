$('.playlist-nav-body li.active').removeClass('active')
$('#playlist-artists').parent().addClass('active');
$('.page-title').text('Artists');
$('.music-wrapper').empty();

$('.music-wrapper').append(`<%= escape_javascript(render partial: 'artists') %>`);
